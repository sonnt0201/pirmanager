import { IGroundService } from "../interfaces";


// export const loadVideo: IGroundService<string, ArrayBuffer> = {
//     channel: "load-video",
//     // raw file name without any extension
//     executer: (_, rawName) => {
//         var fs = require("fs");

//         const stream = fs.createRea
//     }
// }